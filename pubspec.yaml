name: number_magnifier
description: Make numbers in a text appear larger.
version: 0.0.1
homepage: https://github.com/Arouven/flutter-number_magnifier

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
